Source contracts are intentionally minimal for this project.

The deployment scripts use pinned `BurnMintERC20` and `BurnMintTokenPool`
contracts directly from submodules so the generated SecureVal token and pools
match the audited CCIP implementation.
