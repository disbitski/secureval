Source contracts are intentionally minimal for this project.

The deployment scripts use Chainlink's pinned `BurnMintERC20` and
`BurnMintTokenPool` contracts directly from submodules so the generated token
and pools match the audited CCIP implementation.
